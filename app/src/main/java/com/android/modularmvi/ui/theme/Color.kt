package com.android.modularmvi.ui.theme

import androidx.compose.ui.graphics.Color

object Light {
    val primary = Color(0xFF00796B) // Primary color (Teal)
    val onPrimary = Color(0xFFFFFFFF) // Content on primary (White)
    val primaryContainer = Color(0xFFB2DFDB) // Primary container
    val onPrimaryContainer = Color(0xFF00251A) // Content on primary container (Dark)
    val secondary = Color(0xFF009688) // Secondary color (Teal)
    val onSecondary = Color(0xFFFFFFFF) // Content on secondary (White)
    val secondaryContainer = Color(0xFF80CBC4) // Secondary container
    val onSecondaryContainer = Color(0xFF003732) // Content on secondary container (Dark)
    val tertiary = Color(0xFF00897B) // Tertiary color (Teal)
    val onTertiary = Color(0xFFFFFFFF) // Content on tertiary (White)
    val tertiaryContainer = Color(0xFFB2DFDB) // Tertiary container
    val onTertiaryContainer = Color(0xFF003732) // Content on tertiary container (Dark)
    val error = Color(0xFFB00020) // Error color
    val errorContainer = Color(0xFFFFDAD6) // Error container
    val onError = Color(0xFFFFFFFF) // Content on error (White)
    val onErrorContainer = Color(0xFF410002) // Content on error container
    val background = Color(0xFFFAFDFD) // Background (Soft white)
    val onBackground = Color(0xFF191C1C) // Content on background (Dark)
    val surface = Color(0xFFE0F2F1) // Surface color
    val onSurface = Color(0xFF191C1C) // Content on surface (Dark)
    val surfaceVariant = Color(0xFFB2DFDB) // Surface variant
    val onSurfaceVariant = Color(0xFF004D40) // Content on surface variant
    val outline = Color(0xFF4DB6AC) // Outline color
    val inverseOnSurface = Color(0xFFEFF1F0) // Inverse content on surface
    val inverseSurface = Color(0xFF2D3131) // Inverse surface
    val inversePrimary = Color(0xFF004D40) // Inverse primary
    val shadow = Color(0xFF000000) // Shadow color
    val surfaceTint = Color(0xFF00796B) // Surface tint
    val outlineVariant = Color(0xFFB2DFDB) // Outline variant
    val scrim = Color(0xFF000000) // Scrim color
}

object Dark {
    val primary = Color(0xFF80CBC4) // Primary color (Light Teal)
    val onPrimary = Color(0xFF003732) // Content on primary (Dark)
    val primaryContainer = Color(0xFF004D40) // Primary container
    val onPrimaryContainer = Color(0xFFB2DFDB) // Content on primary container
    val secondary = Color(0xFF4DB6AC) // Secondary color (Teal)
    val onSecondary = Color(0xFF003732) // Content on secondary
    val secondaryContainer = Color(0xFF004D40) // Secondary container
    val onSecondaryContainer = Color(0xFFB2DFDB) // Content on secondary container
    val tertiary = Color(0xFF26A69A) // Tertiary color (Teal)
    val onTertiary = Color(0xFF003732) // Content on tertiary
    val tertiaryContainer = Color(0xFF004D40) // Tertiary container
    val onTertiaryContainer = Color(0xFFB2DFDB) // Content on tertiary container
    val error = Color(0xFFFFB4A9) // Error color
    val errorContainer = Color(0xFF93000A) // Error container
    val onError = Color(0xFF680005) // Content on error
    val onErrorContainer = Color(0xFFFFDAD6) // Content on error container
    val background = Color(0xFF191C1C) // Background color
    val onBackground = Color(0xFFE1E3E3) // Content on background
    val surface = Color(0xFF263332) // Surface color
    val onSurface = Color(0xFFE1E3E3) // Content on surface
    val surfaceVariant = Color(0xFF004D40) // Surface variant
    val onSurfaceVariant = Color(0xFF80CBC4) // Content on surface variant
    val outline = Color(0xFF80CBC4) // Outline color
    val inverseOnSurface = Color(0xFF191C1C) // Inverse content on surface
    val inverseSurface = Color(0xFFE1E3E3) // Inverse surface
    val inversePrimary = Color(0xFF00796B) // Inverse primary
    val shadow = Color(0xFF000000) // Shadow color
    val surfaceTint = Color(0xFF80CBC4) // Surface tint
    val outlineVariant = Color(0xFF4DB6AC) // Outline variant
    val scrim = Color(0xFF000000) // Scrim color
}
